GOOGLE_MAPS_API_KEY="your-google-maps-api-key"
ANTHROPIC_API_KEY="your-anthropic-api-key"
PORT=3001
# DB_PATH=./data/venues.db
# TAG_MODEL=claude-haiku-4-5-20251001
# CLIENT_ORIGIN=https://your-frontend.vercel.app   # required in production, see README

# Login via Supabase Auth (optional until you switch; see MOBILE.md).
# SUPABASE_URL=https://your-project-ref.supabase.co
# Only for older projects that sign tokens with a shared secret; newer ones
# publish public keys and need nothing here.
# SUPABASE_JWT_SECRET=
# Lets the server delete a person's Supabase identity when they delete their
# account, and lets scripts/migrate-users.js import existing accounts.
# SUPABASE_SERVICE_ROLE_KEY=
