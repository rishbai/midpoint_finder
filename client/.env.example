# Only needed in production, when the frontend and API are on different
# domains (e.g. frontend on Vercel, API on Railway). Leave unset for local
# dev — Vite proxies /api to the local server, same origin, no config needed.
# VITE_API_URL=https://your-api.up.railway.app
